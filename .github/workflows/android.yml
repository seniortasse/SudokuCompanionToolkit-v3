name: Android

on:
  push:
  pull_request:

jobs:
  assembleDebug:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python/mobile/android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # 🔧 Install Android SDK + cmdline-tools (gives us `sdkmanager`)
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      # Accept licenses & install exact components we need
      - name: Install Android SDK components (platform/build-tools/CMake/NDK)
        shell: bash
        run: |
          set -e
          SDKMANAGER="${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDKMANAGER" --licenses || true
          "$SDKMANAGER" "platform-tools" \
                        "platforms;android-34" \
                        "build-tools;34.0.0" \
                        "cmake;3.22.1" \
                        "ndk;26.1.10909125"

      - name: Export NDK/CMake to env
        run: |
          echo "ANDROID_NDK_HOME=${ANDROID_SDK_ROOT}/ndk/26.1.10909125" >> $GITHUB_ENV
          echo "${ANDROID_SDK_ROOT}/cmake/3.22.1/bin" >> $GITHUB_PATH
          cmake --version

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: ${{ runner.os }}-gradle-

      # 🧹 Fix CRLF + exec bit so Linux can run gradlew
      - name: Fix Gradle wrapper line endings
        run: |
          sed -i 's/\r$//' gradlew
          chmod +x gradlew

      # 📦 Fetch OpenCV Android SDK into the path your CMakeLists expects
      - name: Fetch OpenCV Android SDK
        run: |
          mkdir -p app/src/main/cpp/third_party
          cd app/src/main/cpp/third_party
          curl -L -o opencv.zip https://github.com/opencv/opencv/releases/download/4.10.0/opencv-4.10.0-android-sdk.zip
          unzip -q opencv.zip
          mv opencv-*-android-sdk OpenCV-android-sdk
          test -d OpenCV-android-sdk/sdk/native/jni/include/opencv2

      - name: Assemble Debug
        run: ./gradlew :app:assembleDebug --stacktrace --no-daemon

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/*.apk